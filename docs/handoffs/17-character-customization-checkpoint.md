# 17 — Character customization checkpoint

2026-09-14. The user authorizes committing and normally pushing the reviewed milestone to `git@github.com:ayushrameja/burnhop-rust.git`, branch `main`, then handing CI monitoring back. Earlier task-specific no-save restrictions are superseded. No force push, deployment, additional agents, tasks or watchers.

## Approval and saved scope

The user reviewed the result and said **“looks good”**: user-reported visual approval. This does not mean the user separately completed every persistence, lifecycle, online or physical-input checklist item. The checkpoint brief reports independent manager passes of 142 tests, formatting, strict Clippy and the locked Mac build.

Include the native Character UI, shared articulated preview, bounded offline catalog, versioned local appearance persistence, offline renderer integration, tests and debug-only visual fixtures; preserve the original preset and all online/bot baseline artwork. Include existing context, roadmap, visual direction, inventory, [handoff 16](16-character-customization.md), [24 native PNGs and bounded verification logs](../screenshots/16-character-customization/README.md). No full web-catalog or multiplayer customization claim.

No source/test fixes were needed during this checkpoint. Only context/roadmap and this new checkpoint were edited while saving. Preserve prior documentation and evidence, including historical incomplete results. Personal `appearance.conf`, temporary files, secrets and build output are excluded. The saved user appearance and open game session were not accessed or changed; the sibling browser repository was untouched.

## Repository and validation

Initial branch is `main`; fetched `origin/main` and live remote main both equal local baseline `8a13d129226dbaab80cb2b5775bf8caa78bc39f7`. No incoming commits or unrelated candidate work were found. The final response records the resulting commit SHA, normal-push result, live-remote comparison and working-tree state.

Reused completed validation for reviewed code unchanged during this checkpoint:

- 142 workspace tests: 50 client, 66 core, 18 protocol, 8 server; frozen 12,000-command practice regression passes.
- Formatting, strict Clippy (`cargo clippy --workspace --all-targets --locked -- -D warnings`), locked host and explicit `aarch64-apple-darwin` builds, and headless-server check pass in preserved milestone evidence. The brief separately reports manager validation.
- Fresh `cargo fmt --all -- --check`, Git whitespace checks, candidate-content review and all 24 PNG chunk/CRC integrity checks pass. No private-key/token patterns or personal configuration files found among candidates.
- Core, protocol, server, terrain, online implementation, manifests, lockfile, toolchain and frozen fixtures remain unchanged against the baseline. Source hashes are checked again before saving to detect concurrent edits.

No long gameplay or network rerun was warranted. Ordinary native UI/restart/Host/Join evidence and injected visual fixtures remain distinct from human checklist completion. The combat-script attempt canceled on OS focus loss after initial firing and **remains incomplete evidence**, not a completed seven-checkpoint route.

## New remote CI — pending; user owns monitoring

The normal push triggers the existing **Native checks** workflow. Locate its run for the exact final pushed SHA and verify `headSha`, open the run in the in-app browser, then stop. The final task response supplies the run URL and observed status; if unavailable promptly, supply the SHA and [Actions page](https://github.com/ayushrameja/burnhop-rust/actions). No completion wait or repeated polling.

Monitor both job names:

- `aarch64-apple-darwin` — macOS ARM64.
- `x86_64-pc-windows-msvc` — Windows x64.

**CI gate remains pending until both jobs actually pass for that SHA.** Prior checkpoint CI and local Mac validation do not clear this gate or establish Windows success. If either fails, send the run URL, head SHA, job name, failed step name, first relevant error plus surrounding log lines, and failing test/assertion or compiler diagnostic if present. Steps cover toolchain install, formatting, approved practice regression, lints, workspace tests, headless server and locked executables.

Windows hardware/GPU, second-machine LAN, Mac-to-Windows, real internet/NAT, eight-human feel and unreported checklist subcases remain separately unverified. Customization stays offline-only; further catalog, features and online metadata require separate scope.
