# 14 — Independent Ember Relay offline review

2026-09-13. **Verdict: ready to save. No blocking defects found for the offline save/CI checkpoint.** This is a review of the actual uncommitted implementation on HEAD `7e9610a3d2398e058bfee74bf7fd684b8f0d13fe`, not a claim that these changes have passed CI. Save, push and CI remain separate, explicitly authorized work.

**Findings, ranked by severity — both P3 items fixed on follow-up**

1. **P3 / fixed — cancellation test isolation.** The original test connected/canceled ten times against default `127.0.0.1:5000`, potentially sending undersized teardown packets to a running user's server. [menu_tests.rs:248](/Users/ayushrameja/codebase/LHP/burnhop-native/crates/client/src/menu_tests.rs:248) now binds and retains a test-owned `127.0.0.1:0` receiver, selects its actual address, preserves all ten cancellation/retry cycles and verifies destination, connecting/main state and cleanup. Each cancellation's packet must reach the owned receiver; rebinding the canceled client's source address proves its socket was released. The endpoint is dropped and successfully rebound at the end. Focused and full-suite checks pass. Runtime transport and packet validation remain unchanged. Historical sender attribution remains qualified below.
2. **P3 / fixed — west lower hazard accent.** The original horizontal strip at `(1418,1560)` floated above U04. [terrain.rs:394](/Users/ayushrameja/codebase/LHP/burnhop-native/crates/client/src/terrain.rs:394) now computes the strip's two top heights from U04's authored edge and places its three-unit thickness inside the solid. X1418–1436, color and depth are preserved. This is presentation only: all collision data and approved tuning are byte-for-byte unchanged from the reviewed implementation. The rebuilt native renderer was inspected at 1280×720 and 480×320; the retained screenshots show the accent following the walking edge.

There were no P0/P1/P2 findings in the independent review, and both requested P3 corrections now pass verification. **Ready for the separately authorized save checkpoint; neither fix requires another human playtest.** The detailed original review below remains historical evidence; the final addendum records the subsequent authorized code/test fixes and native captures.

**User approval and original review scope (before the P3 fixes)**

The user reports completing the requested offline playtest and says **“it is working great”**. Record this as user-reported approval of the offline experience. It does not establish Windows, LAN, internet, eight-human balance, or a separately enumerated result for every physical-input subcase.

Read the repository instructions, context/roadmap, approved v2 brief and SVG, implementation handoff 13, and relevant earlier movement/range/combat, multiplayer and menu/hosting handoffs (02, 03, 04, 10, 11). Reviewed tracked diffs and new offline/map/collision/route files, the existing fixed codec and server/prediction paths, tests and preserved logs. No runtime code or tests were edited. Existing work and screenshot bytes were preserved. No agents, commits, pushes, deployment, CI dispatch or CI monitoring. No game-window interaction or new native launch was necessary; the user's UI session was left alone. At initial review time, the required workspace suite contained the pre-existing default-port test; the follow-up now isolates it.

**Geometry, movement and stance**

- Independently parsed the brief's tables against the SVG: **all 35 solids match**. The rerun runtime-to-SVG test compares all 26 rectangles and nine quads exactly. Source inspection confirms the three prescribed exterior boundaries, no global floor, 3200×1900 bounds, floor reference 1640, S0 `(200,1112)`, B0 `(1170,1032)`, and seven inactive candidate spawns. See [ember.rs:3](/Users/ayushrameja/codebase/LHP/burnhop-native/crates/gameplay-core/src/ember.rs:3), lines 34, 198 and 257. Thus **no collision-geometry or approved movement/weapon/fuel tuning deviations were found**. Map validation at line 267 rejects malformed bounds/shapes, interior intersections and unsupported/overlapping role spawns; menu selection at `menu.rs:215` reports failure after resetting to the range.
- [mixed.rs:87](/Users/ayushrameja/codebase/LHP/burnhop-native/crates/gameplay-core/src/mixed.rs:87) sweeps the whole body against X/Y and quad-edge normals. Equal-time upward preference and zero-duration-graze rejection are explicit; broad-phase rejection is conservative. The bounded solver at line 162 preserves commanded horizontal slope displacement, rejects unresolved movement after ten contacts and rechecks support. Downward snap uses the specified `abs(dx)*1.52+.5`, only with prior grounding and nonnegative vertical displacement; jump clears grounding and jet disables the long snap. Rendering alone triangulates the quads.
- Existing new tests meaningfully exercise all nine inclines in both directions, stable support, selected seams, extreme floor/roof/wall/ceiling sweeps and over 1,000 valid randomized displacements. Independent temporary probes added **22 authored seam-direction cases** and an extreme equal-corner contact with reversed solid order; no penetration, cap hit or unexpected blocking remained. An initial overlong standing probe correctly hit T01 at X1250; narrowing it to the actual joint resolved that fixture error without changing implementation. These finite cases do not prove every possible trajectory.
- [offline.rs:33](/Users/ayushrameja/codebase/LHP/burnhop-native/crates/gameplay-core/src/offline.rs:33) applies the exact 68→54.20060507330696, 11-tick feet-anchored transition before movement. Every proposed expansion requires whole-body clearance; both sleeves allow partial expansion below 60 and full standing only after clearing the lip. C/Down, grounded state, held Space, jump edge/buffer and jet intent gate crouching as specified. Grounded full-crouch speed is 160, while airborne tuning stays unchanged. Sleeve/ceiling-thrust tests check retained short height, fuel expenditure and no overlap. Shared cover queries feed shots, bot visibility and barrel clipping (`mixed.rs:246`, `combat.rs:394`, `pilot.rs:248`).

**Recovery and lifecycle ordering**

[combat.rs:601](/Users/ayushrameja/codebase/LHP/burnhop-native/crates/gameplay-core/src/combat.rs:601) advances life first. Only living, non-respawning players reach the pre/post-movement strict `body.y > arena.height` checks at lines 629–645. Pre-crossing skips movement/fuel; post-crossing keeps that tick's fuel effects. The match advances once at line 770. Dead players use the ordinary 180-tick life path, and F5 is handled before it by `step_practice_internal` at line 796.

Recovery at `offline.rs:53` creates supported standing S0 movement, zeroes velocity/stance/buffers/thrust, restores the saved fuel and delay, and leaves combat resources, facing and encounter scores in place. The neutral replacement command suppresses new fire/reload/equip while `Combatant::prepare` advances existing weapon/equip timers once; an already-running reload may complete normally. The bot advances weapon timers without firing during recovery. At tick end `combat.rs:765` sets grace to 180; only subsequent ticks with both actors alive decrement it. Offline pause avoids stepping entirely.

Rerun tests cover both genuine voids, equality/post-crossing versus pre-crossing delay, resource/score retention, reload completion, held-control suppression, neutral regeneration, death precedence and active-map F5. Independent probes additionally verified: an immediately available bot attempt is suppressed on recovery; the first exposed bot shot is exactly the **180th following eligible tick**, without inserting a death/respawn into that countdown; upward jet movement from exact Y1900 does not recover; and F5 above the fall threshold performs a full reset rather than recovery.

The physical client release gate at [main.rs:290](/Users/ayushrameja/codebase/LHP/burnhop-native/crates/client/src/main.rs:290) checks A/D, Space, both Shifts, C/Down, reload, selection, F5 and left mouse, drains events and waits for release. The core also requires a neutral command. Recovery/respawn/reset at line 435 snap both poses/camera, clear buffered aim/input and replace feedback with a new epoch. Core timers and neutral fuel regeneration continue while waiting. No fall heal, score penalty, refill or extra death event was found.

**Camera, cleanup and online compatibility**

The chained client schedule (`main.rs:203`) captures input/aim before simulation and presents afterward. Aim uses the actual last-displayed Bevy inverse projection/GlobalTransform, is recomputed for a stationary cursor, and rejects invalid/outside/stale-size cursor data (`combat_view.rs:149–198`). Ember additionally blocks aim while the camera snap or release gate is pending. Shots use the current body's center. Entry and recovery clear queued targets before the snapped frame; invalid resize/cursor data clears fire edges and requires a fresh click.

`main.rs:545–602` interpolates position and height, follows `top+height−34`, preserves Standard framing/follow rates and clamps Ember's vertical extent to 1735. This algebra preserves the interpolated feet anchor during crouching. Actual Bevy projection tests cover translated cameras, multiple logical sizes, DPRs and alternate scales; dimension tests confirm desktop S0 camera `(640,1146)` and compact clamps. `pilot.rs:287` lowers torso/head/hips and bends the existing rig; standing art retains the previous dimensions. Tests of camera math are not a full render-schedule test or evidence of human aiming feel; source ordering, recorded native resize/fire evidence and the user's approval complement them.

`menu.rs:190–208` discards online/owned sessions, resets map and stance, recovery/review state, world/combat, input/clock and feedback epoch. Practice/Host/Join and the actual connection-start actions all call that reset. `terrain.rs:303` switches fixed terrain pools by map visibility; pose/effect history resets with the epoch. Tests cover Ember→range/Host/Join data isolation, repeated-session cleanup, native menu-event isolation, actual owned UDP host/guest departure/rejoin/stop/rebind. Preserved native evidence independently records guest generations 2 then 3 and fresh range bodies.

Online still uses range prediction (`protocol/src/prediction.rs:117,185`) and server range spawn/step (`server/src/lib.rs:205,277`). The range's original three-pass collision path and exact ray tie path remain selected for empty quad arrays. Codec read reconstructs 36×68 at `protocol/src/codec.rs:190`; validation rejects nonfixed dimensions at line 376. Protocol 2, gameplay `0x4255_524e_0008_0001`, 1,200-byte message/1,185-byte snapshot limits and all protocol/server source are unchanged. Offline stance is not serialized or accepted as an online map selection.

**Diagnostics: what the evidence establishes**

`docs/screenshots/13-ember-relay/evidence/native-desktop.log:118–127` contains ten undersized-packet errors at 13:51:26.499–26.500 UTC. The guest's explicit server-terminated disconnection occurs later, at 13:51:35.706 (`native-guest.log:17–18`), and the host subsequently connects again (`native-desktop.log:130–131`). Therefore “during teardown” is temporal context, not proof those errors are ordinary established-client shutdown traffic.

The locally pinned `renetcode-2.0.0/src/server.rs:389–392` rejects lengths below `2 + NETCODE_MAC_BYTES` (18) **before client lookup or packet decoding**. `packet.rs:354–364` encodes zero sequence with zero sequence bytes. A new `NetcodeClient` begins at sequence zero (`client.rs:114`); its immediate `disconnect()` emits prefix + no payload + 16-byte MAC: **17 bytes** (`client.rs:178`, `packet.rs:217`). Burnhop's unchanged `NetworkClient::drop` calls transport disconnect (`protocol/src/transport.rs:204–205`). The independent ephemeral-socket probe reproduced that exact packet using the current built libraries.

Classification: **a reproducible pre-handshake teardown quirk in the pinned dependency, plus a pre-existing test-isolation issue (now fixed); no demonstrated Ember or established-session networking regression**. The ten-iteration default-port cancellation test is a strong plausible source of this particular burst. Historical sender/packet bytes and timestamped test correlation were not recorded, so definitive attribution remains a validation limitation. Do not label the historical log error-free, assert every normal disconnect is undersized, or weaken the transport guard. If errors recur during ordinary established play outside testing, capture sender/length and lifecycle timestamps in an isolated short reproduction before deciding on a transport change.

`range-route-canceled.log:4` explicitly says `COMBAT PLAYTEST canceled (focus lost)`. `main.rs:260` calls cancellation on focus loss; lines 367–372 mark the route complete and clear input. This is intentional opt-in script behavior, **not a completed seven-checkpoint native run and not evidence of a gameplay failure**. The unchanged seven-checkpoint route passed through the real input buffer headlessly in the workspace suite, and the frozen 12,000-command comparison passed. Earlier native range completion remains historical evidence. No fresh native traversal suite or long network soak was repeated.

**Independent checks completed**

| Check | Result |
| --- | --- |
| `cargo fmt --all -- --check` | Pass |
| `cargo clippy --workspace --all-targets --locked -- -D warnings` | Pass |
| `cargo test --workspace --locked` | **135 passed; zero failed/ignored:** 43 client, 66 core, 18 protocol, 8 server |
| `cargo build --workspace --locked` | Pass; client/server Mach-O arm64 |
| `cargo build --workspace --locked --target aarch64-apple-darwin` | Pass; client/server Mach-O arm64 |
| Locked all-target core/server dependency trees | Core zero dependencies; server no Bevy/window/GPU |
| Geometry cross-check | Independently parsed all 35 brief/SVG solids; runtime/SVG assertions pass |
| Temporary bounded probes | Recovery/grace/F5/equality, 22 seam directions, reversed corner order, ephemeral cancellation packet |
| Diff/preservation | `git diff --check` passes; no source/new-evidence trailing whitespace; no tracked build output; manifests, lockfile, toolchain, CI, protocol/server and frozen reference unchanged |

The new tests add 19 core and five client cases. They assert outcomes such as exact data, support/nonpenetration, body height, resource transfer and timer counts, rather than merely successful execution. Route acceptance uses explicitly injected starting fixtures and prescribed controls; headless and native routes share these fixtures, so their agreement is not two independent route planners. Existing assertions were not weakened: client tests were appended, and the old menu test adds one Tab for the inserted action. `practice_regression.rs:70–87` still compares every normalized Debug field at all 12,000 ticks without new tolerances or exclusions; line 95 retains the original Mac golden. The three frozen SHA-256 values exactly match their README: `0f6cd62a…`, `b496155c…`, `f8237bdb…`.

The temporary probe source/executable stayed in `/private/tmp`; no test file was added to the repository. A 177-file content ledger before documentation edits was used to verify that only the requested context/roadmap changed and this handoff was added.

**Native visual evidence and remaining validation**

Independently inspected the original PNGs **01, 04, 06, 11, 12, 14, 15 and 19** in the milestone 13 screenshot directory: desktop entry, west crouch, lower gaps, minimum-size main menu/help/east crouch/central island and host confirmation. Cool solids, pale tops and darker undersides remain distinguishable from the muted warm background; both gaps stay visually open. Crouching bends the pilot without scaling the whole character. At 480×320 the pilot is necessarily small, but HP/fuel/ammo, all five menu buttons, keyboard hints and both stop-host choices remain legible. The amber-accent issue identified in those original captures is fixed by the follow-up below. The older desktop crouch image predates the documented help-wrap polish; final compact evidence includes that correction.

These are inspections of preserved native framebuffer captures, not new gameplay sessions. Pixels establish presentation at those frames, not motion feel, physical held-input behavior, route fairness or performance. Reported two-window hosting, 26 native stages and both recovery runs remain implementation-agent evidence; source/tests/log inspection here is separate.

**No additional repeat Mac offline playtest is required before saving on the evidence available.** The user's completed playtest supplies the requested feel approval, and no new blocking trigger needs human reproduction. Windows compilation/CI for this change remains outstanding for the separately authorized checkpoint. Later Windows hardware review should specifically exercise crouch/blocked standing, held controls through focus/recovery, high-DPI resize/aim and compact menus because Mac checks do not establish Windows input, windowing or GPU behavior. Second-machine LAN, Mac-to-Windows and internet/NAT/relay validation remain separate and unclaimed. No CI was started or monitored here.


**P3 fixes and verification — 2026-09-13 follow-up**

The user explicitly requested both corrections. Changed only `crates/client/src/menu_tests.rs` and the decorative U04 accent in `crates/client/src/terrain.rs`, plus this handoff, focused context/roadmap status and new native evidence. All pre-existing implementation/design work and milestone 13 screenshots/logs are preserved. No dependency, collision geometry, movement/fuel/weapon tuning, online protocol, transport, frozen fixture, route harness or other runtime behavior changed. No additional agents, commits, pushes, deployment or CI actions.

Cancellation coverage still exercises ten real connect→cancel→retry cycles through the menu actions, without advancing a handshake. A one-second receiver timeout bounds failures. New assertions check the exact owned destination, Connecting→Main transition, removal of online/owned-session/address state, receipt of cancellation traffic on the owned endpoint, client socket release and final endpoint release. The test deliberately does not freeze the dependency's 17-byte packet quirk as a required wire format. Local RAII ownership also cleans resources if an assertion fails. The fix prevents this test from targeting a user game; it does **not** establish that the original ten logged errors came from this test. The historical explanation and missing-sender limitation remain unchanged.

The accent uses U04's existing top vertices `(1300,1640)`→`(1440,1560)` to derive approximately Y1572.57 at X1418 and Y1562.29 at X1436, instead of drawing both ends at Y1560. Its bottom edge is three units farther into the material. No collider or gameplay code was edited, and no test was added merely to repeat this small drawing calculation.

| Follow-up check | Result |
| --- | --- |
| Focused `menu::tests::rejection_copy_is_actionable_and_cancel_allows_a_new_attempt` | Pass; ten owned-endpoint cancellations, state cleanup and socket-release checks |
| Focused `runtime_geometry_matches_every_authored_svg_solid` | Pass; all 35 approved solids unchanged |
| `cargo fmt --all -- --check` | Pass after formatting the new assertion |
| `cargo clippy --workspace --all-targets --locked -- -D warnings` | Pass |
| `cargo test --workspace --locked` | **135 passed; zero failed/ignored** (43 client, 66 core, 18 protocol, 8 server), including the unchanged 12,000-command frozen regression |
| `cargo build --workspace --locked` | Pass |
| `cargo build --workspace --locked --target aarch64-apple-darwin` | Pass |
| Native renderer | Actual rebuilt client / Apple M1 Pro / Metal, desktop 1280×720 and minimum 480×320; both corrected accent captures inspected |
| Diff/preservation | Whitespace check passes; starting 178-file content ledger confirms only the two intended source files and focused documentation changed; core/geometry/tuning/protocol/manifests/lockfile/frozen evidence preserved |

[Desktop screenshot](/Users/ayushrameja/codebase/LHP/burnhop-native/docs/screenshots/14-ember-relay-review/01-west-lip-desktop.png) and [compact screenshot](/Users/ayushrameja/codebase/LHP/burnhop-native/docs/screenshots/14-ember-relay-review/02-west-lip-compact.png) retain the complete native framebuffer, losslessly converted from PPM. The [evidence index](/Users/ayushrameja/codebase/LHP/burnhop-native/docs/screenshots/14-ember-relay-review/README.md) links original native logs and describes the injected fixtures. The pilot stands immediately left of the corrected rim; the amber strip follows the pale walking edge, the void stays open, and compact HP/fuel/ammo remains legible. Runs used the existing debug route only as far as the relevant captures; no complete native traversal rerun or network soak is claimed. Both temporary preview windows were closed; no pre-existing game window was operated. Logs contain the previously observed Winit Destroyed warning on close, with no logged error or panic.

**Human playtest decision:** neither correction requires another human playtest before saving. One is confined to test isolation, and the other is a small decorative alignment verified in the actual renderer at both relevant sizes. The approved collision and gameplay behavior is unchanged, so the user's earlier offline approval still applies. Windows change-specific CI/build and hardware, LAN, Mac-to-Windows and internet validation remain outstanding and separate. **Readiness: ready to save when authorized.**
